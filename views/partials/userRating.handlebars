        <div class="row profile__active">
            <div class="col-3">
                <div class="profile__active__img">
                    <img src="{{this.dataValues.photoUrl}}" />
                </div>
            </div>
            <div class="col-9">
                <div class="confirmation__selection__description">
                    <h3><span>{{this.dataValues.name}}</span> | <span>{{this.Dog.dataValues.breed}}</span></h3>
                    <hr />
                    <p class="text-small">{{this.Dog.dataValues.profile}}</p>
                </div>
                <div class="profile__active__schedule-details">
                    <h4 class="color-primary bold">Returned:</h4>
                    <span id="pickedUpDate">{{this.dataValues.returnDate}}</span>
                </div>
                  {{!-- <p>{{this.ratings[i].review}}</p> --}}
            </div>
               <hr />
        </div>
  
{{!-- 
    <div class="gallery__user-review">
                            <div class="row">
                                <div class="col-3 pr-0 text-center">
                                    <img class="mt-2" src="${dog.ratings[i].user.photo}" width="100" height="70" style="padding-right: 5px;" />
                                    <h5 class="m-0 bold">${dog.ratings[i].user.userName}.</h5>

                                    <img src="images/rating_${dog.ratings[i].score}.svg" width="90%" />
                                </div>
                                <div class="col-9 text-small">
                                    <p>${dog.ratings[i].review}</p>
                                </div>
                            </div>
                        </div>` --}}